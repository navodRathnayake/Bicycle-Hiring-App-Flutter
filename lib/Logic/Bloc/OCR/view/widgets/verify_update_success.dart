library verify_update_success;

import 'package:flutter/material.dart';

class VerifyUpdateSuccess extends StatelessWidget {
  const VerifyUpdateSuccess({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Updated Successfully'),
      ],
    );
  }
}
