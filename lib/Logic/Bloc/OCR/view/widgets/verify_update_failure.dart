library verify_update_failure;

import 'package:flutter/material.dart';

class VerifyUpdateFailure extends StatelessWidget {
  const VerifyUpdateFailure({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Updated Failure'),
      ],
    );
  }
}
