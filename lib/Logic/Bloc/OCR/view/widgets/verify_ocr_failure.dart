library verify_ocr_failure;

import 'package:flutter/material.dart';

class VerifyOCRFailure extends StatelessWidget {
  const VerifyOCRFailure({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('OCR Process Failure'),
      ],
    );
  }
}
